[webhook_plus]

param.http.user = <string>
* Configure the user for the webhook receiver.

param.http.password = <string>
* Configure the password for the webhook receiver.

param.http.url = <string>
* Configure the url for the webhook receiver.

